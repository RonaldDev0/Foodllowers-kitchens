import { Button } from '@nextui-org/react'

export function AddAddressButton () {
  return (
    <div>
      <Button
        color='secondary'
        className='w-96 text-lg font-semibold fixed bottom-5'
      >
        Add Address
      </Button>
    </div>
  )
}
